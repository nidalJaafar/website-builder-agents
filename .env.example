# LLM Provider Configuration
# Options: 'deepseek' or 'lmstudio'
LLM_PROVIDER=deepseek

# DeepSeek Configuration (used when LLM_PROVIDER=deepseek)
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# LM Studio Configuration (used when LLM_PROVIDER=lmstudio)
# Make sure LM Studio is running and serving a model
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=your-model-name-here